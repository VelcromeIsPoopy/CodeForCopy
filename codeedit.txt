import java.util.ArrayList;

public class DataPurge
{
    public static String removeDuplicates(ArrayList<String> arr)
    {
        for(int i = 0; i < arr.size(); i++)
        {
            for(int o = 0; o < arr.size(); o++)
            {
                if(arr.get(i) == arr.get(o))
                {
                    System.out.println(arr.get(o));
                    arr.remove(o);
                }
            }
        }
        return "No duplicates found";
    }

    public static void removeName(ArrayList<String> arr)
    {
        
    }
    
    public static void correctlyFormatted(ArrayList<String> arr)
    {
        
    }
}
